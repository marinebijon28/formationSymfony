{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block content %}
<div class="block-small">
    {% for message in app.flashes('notice') %}
        <div class="alert alert-info">{{ message }}</div>
    {% endfor %}
    <form method="post">
     <h1 class="h3 mb-3 font-weight-normal">Merci de vous connecter</h1>
        <label for="inputEmail" class="connected">Votre email</label>
        <input type="email" name="email" id="inputEmail" class="form-control" placeholder="Votre adresse mail" autocomplete="email" required autofocus>
        <button class="btn btn-lg btn-info btn-block mt-3" type="submit">
            Réinitialiser votre mot de passe
        </button>
        <hr>
        <p class="text-center">Ou souhaitez-vous <a href="{{ path('login') }}">vous connecter</a> ?</p>
    </form>
</div>
{% endblock %}
